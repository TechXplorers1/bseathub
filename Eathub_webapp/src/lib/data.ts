import type { Restaurant } from './types';

// Static mock data has been removed. All data is now fetched dynamically from the API.
export const allRestaurants: Restaurant[] = [];
export const allHomeFoods: Restaurant[] = [];
export const chefs: any[] = []; // If you have a separate chefs array, keep it empty here as well